{
  "name": "scan-to-order-saas",
  "private": true,
  "version": "0.1.0",
  "description": "AI-powered QR scan-to-order SaaS platform for restaurants and small businesses in Pakistan.",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write .",
    "test": "turbo test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.12.12",
    "prettier": "^3.2.5",
    "turbo": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
